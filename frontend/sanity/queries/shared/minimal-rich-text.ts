/**
 * Projection for minimalRichText fields: passes blocks through unchanged.
 * No link annotations to resolve — just bold and italic decorators.
 */
// @sanity-typegen-ignore
export const minimalRichTextQuery = `...`;
